# Server
PORT=3001
JWT_SECRET=change-this-to-a-long-random-string

# Public URL of the web app (for CORS and Socket.io)
CLIENT_URL=

# Serve built client from server (single process for domain deploy)
SERVE_CLIENT=1
NODE_ENV=production
